**整体评价：约 6/10**

代码基础比普通个人项目好，已经是“可运行 Beta”；但工程体系仍偏原型阶段。最明显的差距不是功能，而是测试、模块边界和持续交付。

**1. 构建基础：较好**
- Swift 6、`@MainActor`、`actor APIClient` 都用对了。
- 使用 XcodeGen，工程可以重复生成。
- 我刚用 Xcode 26.3 重新构建，模拟器版本 **构建成功**。
- 目前有两处 Swift 警告，说明还没有做到零警告：`/Users/you/Library/Mobile Documents/com~apple~CloudDocs/Code/Wetrace_Pro_ios/Sources/Views/Report/AnnualReportView.swift:1133`
- 没有把编译警告当错误处理，因此警告容易长期积累。

**2. 架构：基础正确，但边界开始失控**

做得好的地方：

- `WetraceDataSource` 把真实 API 与 Mock 数据隔离，便于预览。
- `APIClient` 使用 actor，网络层线程安全基础不错。
- `AppState` 用 `@MainActor` 管理全局 UI 状态。
- 有统一设计系统、Keychain、缓存命名空间和多账号隔离。
- 24 个 `#Preview`，页面开发体验很好。

问题在于项目扩张后仍接近单层结构：

- `AppState` 已有约 410 行，同时负责账号、配对、TLS、面容解锁、网络状态、缓存范围、统计参数和预加载，正在变成“万能状态类”。
- 绝大多数页面直接读取 `appState.client` 并自行处理网络、缓存、加载状态和错误。
- 只有主页有正式 ViewModel，其他页面把业务逻辑放在 SwiftUI View 内。
- `WetraceDataSource` 已经包含近 30 个接口，任何 Mock 都必须实现全部功能。
- `AnnualReportView` 约 1148 行，模型文件约 628 行，APIClient 约 496 行，已经影响审查和维护。

建议逐步拆成：

- `AccountStore`
- `ConnectivityService`
- `StatisticsRepository`
- `SessionRepository`
- `ImageRepository`
- 各功能独立 ViewModel

不需要大重写，可以在后续修改相关功能时逐步迁移。

**3. 并发管理：有意识，但不统一**

好的部分：

- 搜索会取消旧请求。
- 时区写入已经串行化。
- 多账号切换使用 generation 防止旧账号结果覆盖。
- 多个页面会检查请求序号，避免旧结果回写。

仍然存在的问题：

- 有些页面只防止旧结果写回，但没有真正取消旧网络请求。
- AI 请求、回放加载和后台预加载仍有非结构化 `Task`。
- 年度报告同时存在普通 `.task` 和 `.task(id:)`，首次进入可能发送两次报告请求。
- 回放仍使用持续运行的 Combine Timer。
- 网络探测依赖主页生命周期，而不是独立的连接监控服务。

目前属于“每个 Bug 单独加保护”，还没有统一的任务生命周期方案。

**4. 缓存与性能：设计方向对，执行方式偏简单**

优点：

- 缓存包含账号、年份、时区和排除名单，避免串账号和旧统计。
- 采用“先显示缓存，再后台更新”，体验方向正确。

问题：

- `DiskCache` 同步读取文件并进行 JSON 编解码，调用方大多位于主线程。
- 年度报告存入 `UserDefaults`，不适合长期保存较大的结构化数据。
- 会话、联系人、图片等数据由各页面分别获取，缺少共享内存缓存和请求去重。
- 图片没有统一内存缓存、磁盘缓存和缩略图降采样。
- 后端仍存在全量消息扫描、内存分页及逐联系人查询，这才是大数据量下最严重的瓶颈。

**5. 安全与隐私：明显高于原型，但还没到公开发布标准**

做得比较好：

- Token 存在 Keychain，并使用 `ThisDeviceOnly`。
- 实现了证书 TOFU 锁定。
- 证书变化后需要用户确认。
- API 强制同源，避免把 Token 发送到服务端返回的外部地址。
- 有面容 ID / 密码应用锁。

风险：

- `NSAllowsArbitraryLoads` 仍然开启。
- 配对允许 HTTP，使用 HTTP 时 Token 和数据可以明文传输。
- 开发者密码硬编码在正式源码中。
- 联系人、AI 总结、统计结果等会存入本地缓存，但 README 仍写着“App 不存任何聊天数据”，描述已经不准确。
- 缓存缺少明确的文件保护、过期和容量策略。

**6. 错误处理与诊断：用户提示不错，开发诊断不足**

- `APIError` 能提供中文错误和具体解码路径，这一点很好。
- 但生产代码中仍有不少 `try?`，部分错误会被静默吞掉。
- 缓存写入失败、Keychain 保存失败等情况没有可靠反馈。
- 没有统一日志、请求标识、耗时记录或崩溃信息收集。
- 出现“偶尔数据没刷新”时，目前主要依赖人工复现。

**7. 测试与持续集成：当前最大短板**

目前没有发现：

- 单元测试目标
- UI 测试目标
- GitHub Actions
- 自动构建检查
- 统计结果基准测试
- 代码覆盖率
- SwiftLint 或格式检查

这意味着“现在能编译”无法保证下一次修改仍然能编译，更无法证明统计结果正确。尤其年度统计、时区、跨年、排除联系人和账号切换都非常需要自动化测试。

MockDataSource 已经提供了不错的测试入口，只是尚未真正转化成测试。

**8. 文档与仓库流程：存在明显漂移**

- README 仍描述为 MVP 骨架，只列出主页、年度报告和设置，与当前功能不符。
- README 关于本地数据存储的说明不准确。
- 当前主目录有 21 个文件未提交，约 497 行新增、150 行删除。
- 当前任务 worktree 落后主分支 86 次提交，继续在旧 worktree 开发容易制造冲突。
- 现有提交钩子只负责生成 Xcode 工程，不执行构建或测试。

**最终判断**

这个项目不需要推倒重来。底层方向是正确的，而且近期对并发、账号隔离和证书安全的修复说明工程意识正在提高。

接下来最应该暂停一轮新功能，依次完成：

1. 建立测试目标和自动构建。
2. 给统计功能建立固定数据集与预期结果。
3. 优化后端全量查询和 N+1 查询。
4. 抽出共享 Repository 与异步缓存。
5. 统一异步任务取消和加载状态。
6. 收紧 HTTP/ATS，更新隐私和开发文档。
7. 清理警告并将主分支恢复为干净、可回滚状态。

完成这些后，工程质量可以从“可用 Beta”提升到真正可持续维护的发布级项目。



**对于未来发展的想法采访**



**现有功能地图**

- 数据获取：Windows/macOS 微信数据库密钥提取、V3/V4 数据库解析、解密与增量同步；macOS 暂不支持图片密钥。
- 聊天浏览：会话列表、消息分页、日期跳转、群聊、引用、转账、红包、语音、视频、文件、链接等消息展示。
- 搜索与导出：全文搜索、上下文查看、时间与发送者筛选；HTML、TXT、CSV、XLSX、DOCX、PDF、JSON、取证格式及语音批量导出。
- 联系人：五类联系人筛选、联系人导出、联系提醒、会话类型自动识别与手动修正。
- 统计分析：联系人小时/星期/月度/每日趋势、消息类型、通话、语音、回复速度、互动比例、群成员排行。
- 年度报告：全局或单联系人报告、多时区、同比、字数统计、历史月均、流式生成、前后端缓存、PDF 打印导出。
- 关系洞察：日历热力图、年度关系变化、收发比例、回复速度排行、共同群聊。
- 关系星图：亲密度评分、人生阶段和关系类型、时间轴、年度回忆、时间范围筛选、联系人置顶、人工修正、自定义身份词典。
- 公众号画像：推送数量、活跃/沉默账号、订阅号与服务号、推送时段、标题词频。
- AI：聊天总结、总结历史、待办提取、关键信息提取、情感分析、模拟聊天、语音转文字、自定义提示词。
- 自动化：定时同步、定时备份、关键词/AI 监控、Webhook、飞书、Telegram 告警和 Telegram 远程查询。
- 移动端基础：多设备配对、二维码、Token 鉴权、连通性与配置接口，已经为独立 iOS 客户端铺好了服务端基础。
- 多账号：后端具备添加、切换、删除微信账号的能力，但目前没有完整的前端管理入口。

主要入口可以从 [App.tsx](/Users/you/Code/Wetrace-Pro/ui/src/App.tsx)、[route.go](/Users/you/Code/Wetrace-Pro/web/route.go) 和 [CHANGELOG.md](/Users/you/Code/Wetrace-Pro/CHANGELOG.md) 看到。

**我对当前阶段的判断**

项目功能非常丰富，但“工程稳定性、发布能力、产品聚焦”已经开始落后于功能增长：

- 前端生产构建通过，但主 JS 包约 1.31 MB，还没有按页面拆包。
- `go test ./...` 失败，主要是测试数据库结构没有随当前 schema 更新。
- 前端 lint 有 102 个错误、13 个警告。
- 目前没有常规 push/PR 质量检查，只有打 tag 时的发布流程。
- 代码和界面版本已经是 v2.4.1，但 GitHub 最新可下载 Release 仍是 v1.7.2。v1.8、v1.9、v2.0 的 Actions 因 GitHub 账户账单/额度问题没有启动。
- 详细文档有部分过期，例如仍写 macOS 不能自动提取密钥、年度报告每次重新计算，与当前代码不一致。
- 移动端 Token 当前可以访问几乎全部 API，而不仅是只读查询。正式远程开放前，需要做权限范围、Token 隐藏、吊销和安全审计。
- README 的“完全不上传隐私数据”需要更准确：启用外部 AI、飞书、Telegram 或 Webhook 后，选中的消息内容会发送给第三方。

因此，我暂时不建议继续无边界地增加分析页面。更合理的节奏是：先恢复发布和质量基线，再选择一条明确的产品主线。

**我认为最有潜力的方向**

1. 个人关系管理：把星图、联系提醒、共同回忆、重要日期、承诺/待办和月度关系变化合成真正有持续使用价值的“关系助手”。
2. iOS 伴侣端：优先做总览、年度报告、联系人详情、关系星图和提醒通知，而不是复制整个桌面 Web。
3. 可解释的数据分析：所有统计数字都可以点进去查看来源消息和计算口径，让报告更可信。
4. 隐私 AI：增加本地模型、语义搜索、“询问我的聊天历史”和可选择的数据脱敏。
5. 分享与回顾：生成经过匿名化和隐私裁剪的年度报告、关系纪念册或长图。
6. 运维可靠性：自动健康检查、同步失败原因、数据兼容诊断、备份恢复和版本升级迁移。

**现在需要你回答的问题**

请按编号回答，短答就可以：

1. 这个项目主要是你个人使用，还是准备公开给普通用户下载？未来是否考虑收费？

2. 你最希望用户记住它的哪个价值：聊天备份、数据分析、关系回忆、取证工具，还是手机随时查看？
3. 你自己现在最常使用哪三个页面？哪些功能做完后实际上很少使用？
4. 当前最让你不满意的三个问题是什么？可以是速度、界面、统计不准、操作复杂、平台兼容或缺少功能。
5. 接下来三个月，iOS 客户端是不是最高优先级？首版必须包含哪些页面？
6. 你希望 AI 可以把聊天内容发送给云端模型，还是未来要以完全本地、隐私优先为主？
7. 你更希望关系星图偏向“好看、有回忆感”，还是偏向“准确、能帮助维护关系”？
8. 你是否愿意先安排一个版本只做测试、发布、安全和文档治理，暂时不增加大功能？
9. 主要目标平台是什么：Windows、macOS，还是两者都必须同等支持？你真实数据大约有多少条消息、多少年记录？
10. 在“关系提醒、聊天语义搜索、iOS 看板、分享式年度报告、监控告警完善”中，请排出你的前三名。

你回答后，我会据此整理成明确的产品定位、功能优先级和分阶段路线图。
