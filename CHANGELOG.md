# 开发日志

## v2.0.0 — 2026-08-09

- **关系星图**：全新一级功能,含星图 / 时间轴 / 回忆三种视图
- **关系星图 Phase 1**：新增关系星图入口、后端评分引擎与 Canvas 星图视图，按历史深度、当前温度、综合亲密度、陪伴持续、双向交流等维度展示联系人关系
- **陪伴时间轴**：关系星图新增月度时间轴与总体陪伴热力带，可按首次出现、亲密度、姓名排序，查看每段关系随时间的变化
- **人生回忆模式**：新增年度关系回顾，展示当年陪伴最多的人、新增重要关系、持续陪伴、恢复联系与逐渐淡出的关系
- **年度报告接入关系回顾**：年度报告页可附带关系回顾内容，并随 PDF 导出
- **人工修正**：支持在关系星图中手动调整关系类型、人生阶段、重要标记或隐藏联系人，重建星图后保留修正
- **增量重建**：关系星图重建复用 `raw_features.json` 缓存，只重扫有新消息的联系人，降低重复分析成本
- **显示优化**：星图增加同心距离环、动画开关，并按最新约定调整颜色：朋友为黄色、同学为蓝色
- **年度对比过滤**：年度对比的淡出 / 新进 / 升降名单排除群聊与公众号，只统计真实联系人

## v1.9.0 — 2026-07-31

- **关系洞察页**（侧边栏新增「关系洞察」）：一整页深度社交分析
  - **日历热力图**：GitHub 贡献图风格，全年每天消息密度一目了然
  - **双向互动比**：逐个联系人「你发 vs ta 发」的条数、双方主动发起对话的次数，识别「单向维系」关系
  - **回复速度分析**：你回 ta / ta 回你的平均时延、最快、深夜秒回统计，以及近似「已读不回」
  - **共同群聊**：某位好友与你共同所在的群列表
- **年度横向对比**：`2025 vs 2026` 两年整体指标 + 每位联系人互动量变化（淡出 / 新进 / 上升 / 下降）
- **报告 PDF 导出**：年度报告页新增「导出 PDF」按钮，打印时自动隐藏侧边栏与工具栏、只输出报告主体
- 底层：基于消息方向（`real_sender_id`/`Name2Id`）+ SQLite 窗口函数（`LAG`/`LEAD`）实现，共同群通过 `chatroom_member` 反查；年度对比改用 topContacts 汇总概览，避开整段历史扫描，响应从超时降到约 2 秒

## v1.8.0 — 2026-07-29

- **macOS 原生支持**：Wetrace 现可在 Apple Silicon Mac 上原生编译运行（此前仅支持 Windows），新增 `run-macos.sh` 一键构建启动脚本
- **macOS 密钥提取**：内嵌 chatlog 后端（`internal/cl/`，Apache-2.0），通过 lldb 在系统函数 `CCKeyDerivationPBKDF` 上下断点，于微信 v4（4.1.x）打开数据库、派生密钥的那一刻截获 32 字节数据库密钥——替代对新版微信已失效的内存扫描方案。运行前提：关闭 SIP（`csrutil disable`）+ 微信登录运行
- **平台自动分流**：`GetWeChatDbKey` 按 `GOOS` 用 build tag 自动选择 Windows（DLL 注入）或 macOS（lldb hook）方案；原 Windows-only 的 `key/pkg`、`wxkey` 加 build tag 隔离，两平台互不影响
- **前端同源修复**：API baseURL 改为 `window.location.origin`（原硬编码 `127.0.0.1:5200`），解决服务实际监听端口与前端请求端口不一致时页面一直「加载中」/ Network Error 的问题
- **密钥面板 Mac 引导**：`KeyManagerModal` 在 macOS 下提示关闭 SIP、保持微信登录等操作要点
- **已知限制**：macOS 端图片解密所需的图片密钥（v4 `07085632` 格式）暂未支持——经排查该密钥由微信自研加密实现处理、不经过任何系统加密 API，无法用同一套 hook 手段截获；聊天记录的统计与分析功能不受影响

## v1.7.0 — 2026-06-06

- **多账号管理**：支持在同一台电脑上管理多个微信账号的聊天记录，新增账号列表 API（`/api/v1/accounts`）
- **账号自动检测**：启动时自动扫描常见微信数据目录（`WeChat Files`、`xwechat_files`）下的 `wxid_*` 文件夹
- **账号切换**：通过 API 激活任意已注册账号，后台自动重新解密并重载数据；网页设置页可手动切换
- **安全加固**：CORS 限制为 localhost、API 响应不再返回明文加密密钥、Cookie 设置 httpOnly、`.env` 文件权限收紧为 0600
- **测试修复**：`store_test.go` 改用 Mock 数据，移除硬编码的真实用户路径
- **部署模板**：新增 `.env.example` 供新机器部署参考

## v1.0.7 — 2026-04-26

- **年度社交报告**：消息总数、活跃天数、亮点（最忙、最安静、连续天数、深夜消息）、月度趋势、亲密度排行、24 小时分布、星期分布、消息类型分布
- **年度报告时区设置**：默认时区下拉选择，生成报告时按所选时区聚合（SQLite strftime 加偏移修饰符）
- **批量语音转文字**：聊天页「一键转文字」一次跑完会话内所有语音消息，进度条显示
- **AI 总结自动包含语音文字**：缓存的语音转写结果会被 `[语音] xxx` 形式注入到 `buildChatText` 喂给模型
- **本地数据目录卡片**：设置页显示 `data/` 绝对路径，「在文件管理器中打开」一键直达

## v1.0.8 — 2026-04-27

- **年度报告时区配置持久化**：默认时区 + 所有时区段写入 `localStorage`，下次打开自动恢复
- **多时区段支持**：可指定不同日期范围内分别用不同时区（如 9.10–12.21 UTC、12.22–12.26 UTC+9 等），后端按段拼接 SQL strftime 修饰符聚合
- **生成报告按钮**：修改配置不再自动生成，必须点「生成报告」才发请求

## v1.0.9 — 2026-04-27

- **年度报告 UI 调整**：标题居中、配置面板放宽 `max-w-2xl` 不挤变形
- **时区段重叠修复**：`buildSegments` 用 `cursor` 跟踪已处理边界，重叠 segment 自动裁剪不再双重计数
- **默认时区 bug 修复**：之前 `buildSegments` 用第一个 segment 的时区作为默认，现在用用户在面板里设的默认时区

## v1.1.0 — 2026-04-27

- **联系人统计面板增强**：在 Chat 页面打开数据分析后新增「关键词出现次数统计」「星期分布」「月度消息趋势」「消息类型分布」卡片
- **群聊接收数 = 0 修复**：V4 群聊里 `talker = xxx@chatroom`，原 SQL 用 `n.user_name != talker` 判断"不是自己"，群聊永不命中；现在群聊用 `real_sender_id = 0 || status = 2` 判定自己
- **亲密度排行包含群聊**：去掉 `@chatroom` 过滤，群聊带「群聊」标签
- **排除筛选**：年度报告排除指定联系人/群聊，搜索 + 勾选，存到 localStorage
- **消息类型从多到少排序**

## v1.2.0 — 2026-04-28

- **联系人月度图**：实线为当年每月、虚线为亲密度 Top10 联系人「过去年份的月均」（按月变化），虚线带显示/隐藏开关
- **年度报告月度趋势**：同样支持 Top10 历史月均虚线 + 开关
- **最早/最晚消息新规则**（年度报告）：以每日 07:00 为日界
  - 最早：先在 06:30–07:30 找发送前 ≥4h 无其他消息；无则取 07:00 后第一条
  - 最晚：先在 06:30–07:30 找发送后 ≥4h 无其他消息；无则取 07:00 前最后一条
- **通话时长统计**：解析 type=50 通话消息的 XML（`<duration>` 字段，备用文案兜底），区分语音/视频，统计总次数、接通/未接通、总时长、最长一次、平均时长。每个联系人/群聊有专门的卡片
- **词云词典管理**：词云页右上角加「词典管理」按钮
  - 黑名单（停用词）：增删即时生效（自动 reload）
  - 白名单（自定义词典）：增删保存到 `data/wordcloud_dict.txt`，需重启应用让 gse 重新加载
- **消息类型分布饼图**：在联系人统计中改成「饼图 + 百分比列表」布局，与年度报告一致
- **修复 V4 `local_type` 打包问题**：V4 的 `local_type` 实际是 `(sub_type << 32) | type`，导致出现 21474836529 / 103079215153 等异常类型；现在取低 32 位拿真实类型，未知类型归到「其他」
- **修复 V3 hourly bug**：`%%H` 在非 Sprintf 路径下被 SQLite 当字面量处理，导致全部归到 0 时
- **词云核心问题修复**：`fetchV4GlobalTexts` 解压 zstd 并剥离 `wxid:\n` 群聊前缀，避免发送者 wxid 进入分词；URL/`@提及`/`xxxx@chatroom` 等噪声预处理过滤；全局词云上限从 1000 → 50000，时间范围真正生效

## v1.2.2 — 2026-04-28

- **联系人统计**：通话统计卡片移到关键词统计上方
- **关键词统计**：结果显示的关键词锁定在点击「统计」时的值，输入框继续修改不会影响已显示的结果
- **平均线 bug 修复**：之前 `local_type` 过滤的 SQL 替换出错（`m.(local_type & ...)` 不合法语法），导致 `GetPersonalTopContacts` 静默失败，Top10 历史月均始终为 0、虚线不显示。修复后平均线和开关都恢复
- **最早/最晚消息**：保证两条来自不同的 07:00-day（同一天产生的不再算「年）度跨度」；只有整个数据都在同一天时才回退到允许同日

## v1.2.3 — 2026-04-28

- **年度报告月度图平均线改为「往年月度趋势平均」**：之前误用 Top10 联系人数据。现在后端按默认时区计算 `req.Year` 之前 5 年（最远到 2010）整年的月度趋势，再按月平均；无数据的年份自动跳过
- 数据通过 `AnnualReport.past_years_monthly_avg` 字段返回，前端虚线、开关沿用

## v1.2.4 — 2026-04-28

- **年度报告月度图平均线时间范围可自定义**：标题栏增加 `from` / `to` 年份输入框 + 「重新计算」按钮，默认 2023..reportYear-1。点击后用 `GET /api/v1/report/past_monthly_avg?from=&to=&tz_offset=` 重新拉取，无需重新生成整个年度报告
- 图例标签同步显示当前生效的年份范围（如「2024-2025年月均」）

## v1.2.5 — 2026-04-28

- **概览数据加同期对比百分比**：六个概览卡（消息总数 / 发送 / 接收 / 活跃联系人 / 活跃群聊 / 活跃天数）右上角以上标形式显示相对往年同期的百分比差。当年份是「未完整年」（系统时间在该年内）时，对比基准只取往年同月同日（YTD）以保证公平
- 颜色规则：≥+10% 绿色，≤-10% 红色，中间区间灰色
- 后端新增 `OverviewDeltas` 字段，逐项计算 `(current - past_avg) / past_avg * 100`

## v1.2.6 — 2026-04-28

- **「有效聊天记录起始时间」全局设置**：从年度报告月度图里抽出来，挪到设置页作为全局配置，存入 `.env`（`EFFECTIVE_CHAT_START_YEAR`）
- 月度图原本的 from/to 输入框 + 「重新计算」按钮已移除，仅保留「显示/隐藏往年月均」开关
- 新设置一处生效全部：① 年度报告月度图的往年月均参考线 ② 概览数据的同期百分比对比，下界年份保持一致
- 早于此年份的数据视为缺失，自动从平均计算中排除

## v1.2.7 — 2026-04-28

- **设置变更后局部刷新**：改完「有效聊天记录起始时间」无需重新生成整份报告。后端新增 `GET /api/v1/report/baseline?year=&tz_offset=` 一次性返回当前生效的「往年月均」+「往年同期 overview 平均」
- 前端监听设置变化，自动调用 baseline 端点拿新数据，原报告的 `past_years_monthly_avg` 和 `overview_deltas` 被局部覆盖（其他章节如月度趋势主线、亮点、亲密度排行等保持原数据，避免重新跑昂贵的 SQL）
- **月度图标题显示统计范围**：「平均值统计范围：2023-2025年」直接显示在标题栏

## v1.3.0 — 2026-04-29

- **年度报告流式生成**（Phase 2）。两种显示模式可在生成报告处切换，记忆到 localStorage：
  - **数据流模式（默认）**：UI 骨架先渲染，每个区块（概览/亲密度/月度/星期/小时/类型/亮点）算完一个推一个；六个概览数字使用 ease-out 滚动动画从 0 爬升到目标值
  - **进度条模式**：屏幕中央一条进度条配合「正在分析星期分布... 5/8 (63%)」实时文字
- **后端 NDJSON 流式接口** `POST /api/v1/report/annual/stream`：复用 `GetAnnualReportWithProgress` 的 `ProgressCallback`，每完成一步立即 flush 一行 JSON 事件 `{type: "section", step, current, total, data}`，结束时再发 `done` 携带完整 report
- **前端 hook**：`useReportStream`（fetch + ReadableStream + NDJSON 解析）+ `useCountUp`（requestAnimationFrame 缓动动画）
- 旧的非流式 `POST /api/v1/report/annual` 保留供向后兼容

## v1.3.1 — 2026-04-29

- **修复数据流模式显示「全 0 占位值」的问题**：之前后端任一步骤静默失败导致流提前断开时，前端无 `done` 事件，状态卡在 running，未到达的区块（如「年度亮点」）一直显示 `{date: "", count: 0}` 等占位值，让人误以为数据是 0
- 现在每个区块用「已加载步骤集合」(`loadedSteps`) 判断：未加载 → 显示骨架屏（脉动小圆点 + "加载中..." + 灰条占位），加载完成 → 渲染真实数据
- 流式生成时顶部新增进度条，实时显示「正在统计：年度亮点 + 同期对比  8/8 (100%)」
- 流提前断开（无 `done`）时前端检测并标记为 `error` 而非永远 running，并在浏览器控制台 `console.error` 提示
- 调试日志：每条 NDJSON 事件解析后 `console.debug("[ReportStream]", ...)`，浏览器 DevTools 即可看到流是否真的逐条到达
- fetch 请求 header 加 `X-Auth-Token`，之前缺漏导致开了密码保护的实例直接 401

## v1.3.2 — 2026-04-29

- **配置未变化时跳过重新生成**：点「生成报告」时对比当前 (年份 + 默认时区 + 时区段 + 排除列表) 与上次成功生成时的签名。完全一致 → 直接复用现有报告，提示「配置未变化，已复用上次生成的报告」，不再花 5–15s 重跑后端 SQL
- 配置任何一项有变 → 正常走流式重新生成

## v1.3.3 — 2026-04-29

- **多份报告缓存（按签名分别保存）**：之前修的「单缓存」对换年份场景失效。现在每次生成成功都把 `(签名 → 完整报告)` 存进 `localStorage`（保留最近 5 份）
  - 切换年份/时区段/排除列表配置后，再切回任何已生成过的配置 → 命中缓存秒开，不重跑后端
  - 启动应用后第一次进入年度报告页 → 直接显示最近一份缓存的报告，无需先点「生成报告」
- 缓存键 `annual_report_cache_v2`，每条带时间戳便于按 LRU 清理
- 数据派生顺序：当前流的 `fullReport` → 流式运行中的 partial → localStorage 中匹配当前签名的缓存 → null

## v1.3.4 — 2026-04-29

- **报告生成中切换年份的正确处理**：
  - 切到「已缓存」配置 → 立即从缓存渲染 + 顶部提示「后台仍在生成 XXXX 年报告」；旧流继续跑，完成后**正确**写入它自己的缓存（之前会错误地写到当前显示的那个 sig 下）
  - 切到「未缓存」配置 → 旧流被 abort，新流接替（串行替换，不并行）
- **流的 params 快照**：`useReportStream` 内部存了启动时绑定的参数，`useEffect` 写缓存时用流自己的 sig 而不是组件最新 sig，根除写错缓存的 bug
- **数据派生隔离**：`stream.fullReport` 必须签名匹配当前 `params` 才会用来渲染，否则回落到 `localStorage` 缓存。再也不会出现"刚切到 2024 看了一眼，2025 流刚好跑完，画面突然跳成 2025"的尴尬情况
- 骨架屏只在「流绑定的配置 == 当前显示配置」时显示

## v1.3.5 — 2026-04-29

- **诊断 + 调试工具**：「报告显示的不是当前年份 / 改了配置依然命中缓存」类问题的定位手段
  - 输入框年份和当前显示报告的年份不匹配时：输入框出现琥珀色环，「生成报告」按钮变橙色 + 显示「(待应用)」 — 提醒用户必须点按钮才会切换
  - 缓存命中的 toast 改为「已复用 XXXX 年的缓存报告」，明确告知是哪一年的
  - 「生成报告」处理逻辑增加 `console.group` 详细日志：当前 inputYear、解析后的 year、defaultTz、segments、excludeTalkers、生成的 sig、所有缓存条目的 sig + 各自 report.year
  - 配置面板加「清缓存(N)」按钮：直接清空 localStorage 的 `annual_report_cache_v2`，调试时方便从干净状态开始

## v1.3.6 — 2026-04-29

- **修缓存竞态污染 bug**：日志看到「sig 是 2026 年但 report.year=2023」，根因是旧流（2023）尚未结束就被新流（2026）替换，旧流的 done 事件晚到，setState 时把 2023 的 fullReport 写到已经是 2026 params 的 state 上，再被 useEffect 错存为「sig:2026 → report:2023」
- 修复路径：
  1. `useReportStream` 在 fetch 循环每次 read 前/后、以及处理 NDJSON 每行前都检查 `controller.signal.aborted`，已被 abort 的流立即 return，不再处理任何遗留事件
  2. `useEffect` 写缓存时双校验：`stream.params.year === stream.fullReport.year`，不一致直接拒绝写入
  3. `findCache` 校验：sig 里的 year 必须等于 report.year，否则视为污损条目跳过
  4. 启动时扫描 localStorage，自动清理已存在的污损条目（之前竞态写进去的脏数据），并控制台输出清理数量

## v1.4.0 — 2026-04-29

- **联系人侧分析使用全局默认时区**：之前所有「24 小时活跃 / 星期 / 月度 / 每日 / 年-月」查询都硬编码 `'localtime'`（服务器时区），跨时区用户数据全错位
  - `Repository` 加 `defaultTzM` 字段 + `SetDefaultTzModifier` 方法，所有 `'localtime'` 引用全部替换为动态读取
  - 新增 viper 键 `DEFAULT_TZ_OFFSET_MINUTES` + `DEFAULT_TZ_OFFSET_SET`
  - 启动时把配置同步到 Repository；用户改完立即热更新（无需重启）
  - 新接口：`GET/POST /api/v1/system/default_timezone`
- **设置页新增「默认时区」卡片**：下拉选 UTC-12 ~ UTC+14（含半时区），影响联系人侧的全部分析
- **联系人月度图：多年勾选**：之前只能看当年。现在数据中存在的所有年份都列出来作为复选框，支持同时显示多条线对比；颜色按年份升序稳定分配。原有「Top10 历史月均」虚线开关保留
- **每日消息趋势加日均参考线**：图表上方新增「活跃天数 / 日均 / 最高单日」三个数字摘要；图上有紫色虚线水平参考线表示日均，可勾选关闭。日均 = 总消息数 / 实际活跃天数（非日历总天数，更反映高峰时的状态）

## v1.4.1 — 2026-04-30

- **字数统计 API + 点击切换显示**：在年度报告里点「消息总数 / 发送消息 / 接收消息」任一卡片，或亲密度排行任意一行的「发送 X / 接收 Y」小字、右侧总数 → 切换显示字数；再点恢复消息数
- 后端：
  - 新增 `model.WordCountStat` / `model.ContactWordCountStat`
  - `repo.GetAnnualWordCounts`：基于 `buildSegments` 同样的时区段处理，仅统计 type=1 文本，按 `length(CAST(message_content AS TEXT))` 算字符数。群聊/私聊的发送/接收判定与亲密度排行保持一致
  - 端点 `POST /api/v1/report/word_count`，请求体与年度报告同
- 前端：
  - 字数请求懒加载 — 任一处需要切换时才发；按当前 sig + react-query 缓存（同配置不重复请求）
  - 概览卡可点击切换：总消息↔总字数、发送消息↔发送字数、接收消息↔接收字数；同期百分比上标在字数模式下隐藏（avoid 误导）
  - 排行行可独立切换：「发送 1234 / 接收 567」↔「发 12345 字 / 收 6789 字」，右侧总数同步换成「总字数」标签

## v1.4.2 — 2026-05-16

- **报告缓存升级：数据指纹校验 + 无上限**
  - 后端新增 `Repository.GetDataVersion()`：扫所有消息 DB 文件的 `(path, size, mtime)` 哈希为 32 字符 md5。新接口 `GET /api/v1/system/data_version` 返回当前指纹
  - `AnnualReport` 响应里包含 `data_version` 字段；流式版和普通版都写入
  - 前端缓存项结构升级为 `{sig, dataVersion, report, ts}`（key 升级到 `annual_report_cache_v3`，旧 v2 自动失效）
  - 缓存命中条件：**sig 配置相同 AND dataVersion 数据指纹相同**。数据库一更新 → 旧缓存自动失效，下次点生成会重跑后端
  - 移除「最多 5 份」上限，所有不同配置 × 不同数据版本的报告都保留；localStorage 满了会 warn 但不报错
  - 同 sig 不同 dataVersion 视为独立条目（保留历史快照，但默认命中最新指纹的）

## v1.5.0 — 2026-05-17

- **Telegram Bot 远程查询**：在 Telegram 给 bot 发命令即可远程拉数据，不需要打开 Web UI
- **架构**：新建 `internal/telegram` 包
  - `Bot` 长轮询 `getUpdates`（30s 超时，错误指数退避）
  - 命令注册表 + helpers（模糊匹配会话名、时间段解析、消息类型中文名、千分位、时长格式化）
  - 复用 `pkg/util.TimeRangeOf` 解析 `7d` / `last-month` / `2024` / `2024-01:2024-12` 等
  - 单条回复 4000 字符截断（Telegram 4096 上限）
  - 白名单 chat_id 防陌生人滥用
- **可用命令**：
  - `/help` — 命令清单
  - `/status` — 系统状态（数据指纹/会话数/最近消息时间/内存）
  - `/list [关键词]` — 列出匹配的会话，最多 30 条
  - `/stat <名字> [时间段]` — 概览：总消息/活跃天/日均/首末日
  - `/words <名字>` — 字数统计（仅当年）
  - `/calls <名字>` — 通话次数/总时长/语音/视频/最长/平均
  - `/types <名字>` — 消息类型分布（前 8 种 + 百分比）
- **配置**：`TelegramConfig` 加 `BotChatEnabled` + `AuthorizedChatIDs []string`
- **生命周期**：API 启动时根据配置启停 worker；用户在监控页保存配置后自动 hot-reload
- **UI**：监控页 → Telegram 平台配置 → 新增「启用 Bot 交互查询」开关 + 白名单 chat_id 文本框（每行一个）+ 命令清单提示

## v1.5.1 — 2026-05-17

- **Telegram bot 新增 `/list N` 排行命令**：参数为纯数字 N 时（1–50），返回亲密度 Top N（按总消息数）
- 例：`/list 10` → 前 10 名「老婆 5,234 条（发 2,100 / 收 3,134）」「工作群 [群] 3,890 条」...
- 其他用法不变：`/list` 列最近 30 条，`/list 张三` 按关键词过滤

## v1.5.2 — 2026-05-17

- **联系人管理被 200 条上限截断的修复**：全局 axios 拦截器对所有 GET 请求强制注入 `limit: 200`，导致联系人列表最多只取 200 条。`getContacts` 现在显式传 `limit: 100000` 覆盖默认值，所有联系人都会拉到
- **联系人类型分类完善**：识别 `@openim` 和 `数字@xxx` 为企业号（之前都被归到「私人好友」）
- **筛选 tab 扩展**：全部 / 私人好友 / 群聊 / **公众号** / **企业号** 五个 tab，每个 tab 右侧带实时数字
- **企业号徽章**：紫色 [企业号] 标签

## v1.6.0 — 2026-05-17

- **移动端 API 鉴权**（为 iOS 客户端准备）
  - 新增持久化的「移动端 API Token」（存 `.env` 的 `MOBILE_API_TOKEN`）
  - 端点：`GET/POST/DELETE /api/v1/system/mobile/token`、`GET /api/v1/system/mobile/ping`
  - 鉴权中间件升级：设置了密码 **或** 移动端 token 时即生效；接受 Web 会话 token **或** 移动端 token 两种凭据
  - iOS App 配对后用 `X-Auth-Token: <移动端token>` 访问全部 API
- 配套独立仓库 `WetraceProiOS`：iOS 26 / SwiftUI / Liquid Glass 客户端骨架

## v1.6.1 — 2026-05-17

- **设置页新增「iOS App 配对」卡片**：完成移动端配对闭环
  - 填入公网访问地址（Tailscale / 内网穿透地址）
  - 一键生成 / 重新生成 / 吊销移动端 token
  - 自动渲染二维码（内容 = `{url, token}` JSON），iOS App 扫码即连
  - 附「手动配对信息」可展开，复制地址 + token 手动输入
- 前端依赖加 `qrcode.react`

## v1.6.2 — 2026-05-17

- **修复：生成移动端 token 后电脑端 Web UI 被锁死的 bug**
  - 根因：鉴权中间件在「设了移动端 token」时拦截所有 `/api/` 请求，连管理 token 的接口本身也被拦。本机 Web UI 没密码、没 token → 全部 401 → 配对卡片读不到状态、二维码不显示
  - 修复：本机（loopback 127.0.0.1/::1）访问 + 未设密码时放行。移动端 token 只防远程访问，本机始终可信。用 `RemoteAddr`（真实 TCP 对端，不可伪造）判断 loopback，不用受 `X-Forwarded-For` 影响的 `ClientIP`
  - 设了密码的情况不受影响：本机也照样要密码
- **配对卡片公网地址自动预填**：默认用当前网页的访问地址（非 localhost 时），局域网场景生成 token 后二维码立即出现

## v1.6.3 — 2026-05-17

- **移动端配对改为「多设备 + 历史记录」**
  - 后端：`MobilePairingStore` 持久化到 `data/mobile_pairings.json`，每条记录含 id / token / 备注名 / 创建时间 / 最后访问时间
  - 每次「新建配对」生成一条独立记录（一台设备一条），不再覆盖
  - 鉴权中间件校验 token 时遍历所有记录；命中后更新该记录的「最后访问」（节流 60s）
  - 旧的单 token（`.env` 的 `MOBILE_API_TOKEN`）启动时自动迁移成一条记录
  - 端点：`GET/POST /api/v1/system/mobile/pairings`、`DELETE/PUT /api/v1/system/mobile/pairings/:id`
- **设置页配对卡片重做**：配对历史列表，每条可重命名（失焦保存）、展开看二维码、删除；显示创建时间和最后访问时间

## v1.6.4 = 2026-05-20 (未改动)
1. 在年度报告中 消息总数的右上角 和下面的卡片信息 可以加上这些百分比(显示对应的颜色) 
2. 后端调整活跃天数的逻辑 不再是一年的开始计算 而是从加上好友(即有第一条聊天记录)  
3. 联系人年度报告中每日消息趋势长按显示具体数值
4. 启用软件时 后端生成亲密度Top前5的报告(注意分配内存 优先级 避免数据错乱)
5. tab bar联系提醒换到 Home右上原来联系人年度报告的位置 删除原来的按钮 下面左边联系人年度报告的图标换成上面原来的. 把更多里面的年度报告 移到原来联系提醒的位置 
6. 当后端没有数据变动 也没有配置变动的时候进入/切换年度报告时 使用原来的报告 而不是重新生成一份(节省服务器性能)
7. 应用端 图片功能 查看图片的时候下面需要写这张图片是和谁 以及什么时候
8. 传递联系人年度报告消息类型的时候 使用合适的中文 (emoji可以不翻译)
