# Changelog

## 1.0.5 — 首个公开版本

- 许可证换成 **AGPL-3.0**（原来的 CC BY-NC-SA 与继承自 Apache-2.0 的文件冲突，
  CC 官方也明确不建议用于源代码）。
- 新增 `NOTICE`：按 Apache-2.0 §4 的要求，写清继承自 chatlog 的部分、原作者署名，
  以及本项目做过的主要改动（移除密钥提取、数据库解密、进程访问与目录同步）。
- README 的「法律与许可」改成简短版：只说清楚产品边界、许可证、署名和使用者责任。


## 1.0.4 — 公众号画像能用了；删掉继承来的解密代码

### 修复

- **公众号画像对导入数据永远是空的**：它只找独立的 `biz_message_*.db`，而导入进来的数据
  全在普通消息库里。现在找不到公众号库就回退到普通消息库，并且只统计订阅号 / 服务号类型的会话
  （演示数据：3 个号、2,353 条推送）。

### 清理：真的不碰解密了

Plus 的定位是「只导入、只分析」，但从上游继承来的 `.dat` 图片解密代码一直躺在仓库里
（`pkg/util/dat2img`、`pkg/util/data2img`，约 1700 行，含 AES / XOR 解密和图片密钥校验），
只是启动时没有传密钥，走不到而已。这轮整个删掉：

- 删除 `pkg/util/dat2img`、`pkg/util/data2img` 两个包
- 媒体服务去掉 `ImageKey` / `XorKey` 字段与 `.dat` 分支，只读导入包里真实存在的文件
- ffmpeg 路径查找抽成 `pkg/util/ffmpegpath`（视频转码还要用）
- 设置页底部那条「需要从微信提取、解密或同步数据？前往 Wetrace-Pro」的外链去掉

现在仓库里搜不到任何提取密钥或解密微信文件的代码。


## 1.0.3 — 「最晚一条」再收紧一次

v1.0.2 把最晚候选放宽成「后面静默 4 小时的最后一条」之后，清晨 06:5x 那种
「醒得早、发一条又没下文」的孤立消息仍然会赢 —— 07:00 是日界，它的偏移最大。
现在要求这条消息**前面没有**静默 4 小时，也就是必须是一段对话的结尾。

真实数据上 2024 年从 06:59 变成 03:47；演示数据是 02:41，没有变化。
（与 Wetrace Pro v2.7.18 同一处改动。）


## 1.0.2 — README 重写（含截图）+ 四个统计 bug

README 重写成一份完整介绍：15 张功能截图、完整功能清单、法律与许可说明、统计口径要点、
项目结构和 17 篇文档的索引。截图全部来自新增的演示数据，不含任何真实聊天内容。

### 新增

- `scripts/gen_demo_import.py`：生成完全虚构的演示导入文件（22 位联系人 + 5 个群 + 3 个公众号，
  跨 4 年 25 万条，含语音、通话、图片占位），固定种子可复现。刻意做出作息缺口、逐年增长、
  年中出现的新面孔、逐渐淡出的人和两位「电话党」，让每个统计页面都有内容可看。

### 修复

- **关系洞察整页白屏**：`year_compare` 的 `faded_out` / `newly_active` 为空时返回 `null`
  （Go 的 nil 切片），前端 `.slice` 直接 TypeError。后端改成返回 `[]`，前端同时兜住空值。
- **没配时区时口径不一致**：`resolveTzMinutes` / `resolveTZ` 未配置时兜底到 0（UTC），
  而 store 层用系统时区，同一份数据两套数字（实测 2024 年年报与年度对比差 8 条、活跃天数差 1 天）。
  现在统一兜底到本机时区。
- **年度亮点的「最早 / 最晚一条」永远是 06:30 / 06:59**：候选被限制在 06:30–07:30 那个窗口里，
  而 07:00 是日界，于是一年的数据必然收敛到日界前后。现在最早取「07:00 后、前面静默 4 小时的第一条」，
  最晚取「后面静默 4 小时的最后一条」，演示数据上是 07:00 / 02:41。
- **星期分布的最后一根柱子显示「Day 7」**：后端给的是 1=周一…7=周日，前端却用 0 基数组，
  周日取到 `undefined`；周末高亮也跟着错了一格。
- **视频通话永远是 0**：V4 新版 `voipmsg` 用 `<room_type>` 区分（0=视频、1=语音），解析器没读这个字段。
- **会话列表露出 XML**：非文本消息的预览直接截断原始内容，现在按类型显示 `[图片]` `[语音]` `[视频]` 等占位。


## 1.0.1 — 「关系回顾」按钮看起来失灵

没设过出生年月时，`/api/v1/galaxy/graph` 返回 409 `need_profile` —— 这是预期状态，不是故障。
但年度报告只是 `catch(() => setGalaxy(null))` 静静吞掉，于是「关系回顾」按钮点了没有任何反应，
控制台还留一条红色报错，新用户不知道缺什么。

现在区分三种情况并给出可操作的提示：没设出生年月 → 「设置出生年月后可用」+ 一键去关系星图；
接口出错 → 说明稍后重试；数据不足 → 说明这一年互动太少。按钮本身也带上同样的 title，
提示卡片不进 PDF。预期内的 409 不再按错误级别打进控制台。


## 1.0.0 — Wetrace Plus 首个版本

### 从 Wetrace-Pro 同步的分析改进

- 新增联系人记忆，并把模拟对话升级为可复用的会话。
- 统一时区配置入口和年度报告口径，优化当月实际值与预计值的连接方式。
- 语音转写改用无损 WAV，浏览器播放用 MP3 提升至 64 kbps。
- 公众号画像不再暴露无意义的统计范围开关；模块开关统一为“开启即跟随全局”。

### 新增

- 新增独立「导入」首页，支持拖放多个 JSON、CSV 或 ZIP 文件。
- 新增 chatlog JSON、chatlog CSV、MemoTrace / WeChatMsg 会话 CSV 与全量 CSV 适配器。
- 新增 chatlog-keeper `wechat_messages.json` 专用适配器，支持浮点时间戳、多账号会话隔离与来源 ID 保留。
- 新增 Wetrace Plus JSON v1 交换格式。
- 新增统一导入事务、消息指纹去重、来源哈希、告警和导入历史。
- 导入后自动刷新聊天、联系人和全部分析能力。

### 产品边界调整

- 移除微信客户端与进程探测。
- 移除数据库及图片密钥获取。
- 移除微信数据库解密。
- 移除账号源目录管理、源目录同步和图片目录预加载。
- 设置页、聊天页、隐私说明和文档全面改为“只导入、只分析”。
- 统一产品版本为 1.0.0，重排侧栏底部工具，并修复桌面与移动端长页面滚动。

### 保留

- 聊天浏览、搜索、联系人、年度报告、词云、关系洞察、回放和多格式导出。
- 可选 AI、语音转文字、监控、备份、密码保护和移动端只读访问。

完整导入规范见 [docs/13-导入格式.md](docs/13-导入格式.md)。
