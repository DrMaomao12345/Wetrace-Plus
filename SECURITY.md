# 安全策略

Wetrace Plus 处理的是聊天记录，任何能读到它的漏洞都按高危对待。

## 报告漏洞

**请不要公开提 issue。** 在仓库的 **Security → Report a vulnerability** 里私密提交，
写清楚：

- 受影响的版本（设置页左下角，或 `VERSION` 文件）
- 复现步骤，最好附一个最小的导入文件或请求
- 你认为的影响范围

收到后会尽快确认；修复发布前请不要公开细节。

## 威胁模型

默认部署是「单人、单机」：服务只监听 `127.0.0.1`，本机访问不需要密码。
在这个前提下，下面这些仍然算漏洞：

- 你打开的**任何网页**能读取或修改数据（DNS 重绑定、跨站请求、XSS）
- **导入的文件**或**别人发来的消息**能让程序读写数据目录以外的文件、访问任意网络地址、执行任意程序
- 开启远程访问后，**没有凭据**的请求能读到数据；移动端配对能修改设置
- `.env`、配对 token、分析库对同一台机器上的**其他用户**可读

下面这些不算：

- 已经拿到本机当前用户权限的攻击者（他本来就能直接读文件）
- 你自己在设置里填了恶意的 AI / Webhook 地址
