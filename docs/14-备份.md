# 备份

备份功能把**已经导入 Wetrace Plus 的数据**再次导出为独立文件，用于归档或离线阅读。它不会从微信客户端同步或补取数据。

在「设置」的备份区域可以：

- 启用定时备份并设置小时间隔
- 指定本地保存目录
- 选择 HTML、TXT 或 CSV 格式
- 立即备份全部会话或指定会话
- 查看最近备份状态与历史

如果希望分析库包含最新消息，请先在原导出工具中生成新文件，再回到「导入」页面导入。重复消息会自动跳过。

备份文件与原始导出文件同样包含隐私数据，应存放在受保护的位置。
